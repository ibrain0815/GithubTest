# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

This is a minimal web project currently containing only an empty `index.html` file. No build system, package manager, or framework is configured yet.

## Repository Structure

- `index.html` — Entry point (currently empty)
- `.claude/settings.local.json` — Claude Code local permissions
